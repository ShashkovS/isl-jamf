cask_args appdir: "/Applications"

brew "ghostscript"

cask "visual-studio-code"
cask "tex-live-utility"
cask "skim"

# Optional supported standalone editor:
# cask "texshop"

# TeXstudio is intentionally omitted while its Homebrew cask is disabled
# because the application fails Gatekeeper checks.
